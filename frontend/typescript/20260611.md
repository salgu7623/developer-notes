
# 목표
생성된 프로젝트 폴더 내의 스켈레톤 파일들을 직접 완성하고, TypeScript의 핵심 개념(제네릭, 유니온 타입, 인터페이스 등)을 실무 관점에서 체득합니다.

# 실무 관점
실무에서는 팀의 시니어 개발자나 아키텍트가 잡아둔 초기 뼈대 코드(Boilerplate)나 베이스 아키텍처를 빠르게 파악하고, 그 위에서 본인이 맡은 비즈니스 로직(타입 및 DTO)을 팀의 컨벤션에 맞게 채워 넣는 적응력이 매우 중요합니다. 다른 사람이 설계한 구조의 의도를 파악하고 코드를 작성하는 훈련입니다.

# 설계
## 폴더 구조: 열어두신 commerce-platform/src/types/ 하위 파일들을 중점적으로 다룹니다.
컴포넌트 구조: (현재 단계 해당 없음 - React 단계에서 설계)

## API 구조: api.ts에 정의될 제네릭 래퍼가 전체 시스템의 API 통신 규약이 됩니다.
데이터 흐름: 작성하실 DTO와 Domain 타입이 클라이언트와 서버 간 데이터가 흐르는 파이프라인의 안전망이 됩니다.
상태 흐름: (현재 단계 해당 없음)

## TODO 구현 가이드
에디터에서 아래 파일들을 차례대로 열고, 주석 처리된 [핵심 코드 힌트]를 참고하여 주석 밖의 실제 코드로 타이핑 및 구현을 진행해 주세요. (기존 주석들은 학습용이므로 지우셔도 되고 남겨두셔도 무방합니다.)

## TODO 1: TS 옵션 구성

[tsconfig.json](file:///C:/Users/skkim8/.gemini/antigravity-ide/scratch/commerce-platform/tsconfig.json) 파일을 열고 엄격한 타입 옵션들을 살펴봅니다.

## TODO 2: 공통 API 규약 작성

[api.ts](file:///C:/Users/skkim8/.gemini/antigravity-ide/scratch/commerce-platform/src/types/common/api.ts) 파일을 열고 ApiResponse<T>, PaginatedData<T> 제네릭 인터페이스를 완성합니다.

## TODO 3: User 도메인 작성

[user.ts](file:///C:/Users/skkim8/.gemini/antigravity-ide/scratch/commerce-platform/src/types/domain/user.ts) 파일을 열고 UserRole, User, SignUpRequest 타입을 완성합니다.
## TODO 4: Product 도메인 작성

[product.ts](file:///C:/Users/skkim8/.gemini/antigravity-ide/scratch/commerce-platform/src/types/domain/product.ts) 파일을 열고 선택적 프로퍼티를 활용한 ProductStatus, Product, CreateProductRequest 타입을 완성합니다.
작업을 모두 마치고 저장하신 후, **"작성 완료했습니다"**라고 알려주시면 변경된 코드를 제가 직접 읽어보고 시니어 엔지니어 관점에서 코드 리뷰를 진행하겠습니다.

# 셀프 체크
코드 작성이 끝나면, 앞서 드렸던 핵심 질문들에 대해서도 본인만의 답을 고민해 보시기 바랍니다. 완벽한 정답이 아니어도 좋습니다. 본인의 언어로 편하게 답변을 적어주시면 피드백 드리겠습니다.

strict: true 옵션이 막아주는 대표적인 런타임 오류(예: undefined 에러)는 어떤 것이 있는가?
만약 백엔드에서 에러 발생 시 data 필드를 아예 주지 않는다면, 제네릭으로 만든 ApiResponse<T> 의 data 필드 처리를 어떻게 수정해야 가장 안전한가?
인터페이스에서 export 키워드를 붙이지 않으면 어떤 일이 발생하며, 실무에서 export와 export default의 차이는 무엇인가?
DTO(Data Transfer Object)와 Domain Entity를 분리했을 때 얻는 가장 큰 장점은 무엇인가?
type 키워드로 생성한 유니온 타입('USER' | 'ADMIN')은 자바스크립트로 빌드되었을 때 어떤 형태로 남게 되는가?
